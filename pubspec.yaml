name: premium_music_player
description: "Stable Premium Music Player"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.36
  on_audio_query: ^2.9.0
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
